apiVersion: ai.sap.com/v1alpha1
kind: Application
metadata:
  name: gemini-llm
spec:
  services:
    - name: llm-service
      template:
        name: huggingface-text-gen
        version: "1.0.0"
      inputs:
        image: "ghcr.io/sap-samples/huggingface-text-gen:latest"
        parameters:
          model_id: "google/flan-t5-base"
